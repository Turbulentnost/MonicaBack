SECRET_KEY=change-me-in-production
DEBUG=True
ALLOWED_HOSTS=localhost,127.0.0.1

POSTGRES_DB=monica
POSTGRES_USER=monica
POSTGRES_PASSWORD=monica_secret
POSTGRES_HOST=localhost
POSTGRES_PORT=5433

REDIS_URL=redis://localhost:6380/0

MINIO_ENDPOINT=localhost:9010
MINIO_ACCESS_KEY=minioadmin
MINIO_SECRET_KEY=minioadmin123
MINIO_USE_SSL=False
MINIO_BUCKET_AVATARS=user-avatars
MINIO_BUCKET_CHAT_FILES=chat-files
# Публичный host:port MinIO для телефонов в LAN (не localhost)
MINIO_PUBLIC_ENDPOINT=192.168.1.157:9010

EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_HOST_USER=
EMAIL_HOST_PASSWORD=
EMAIL_USE_TLS=True
DEFAULT_FROM_EMAIL=Monica <noreply@monica.local>

# Регистрация идёт по email (SMTP ниже). Phone/Telegram OTP временно отключены.

CORS_ALLOWED_ORIGINS=http://localhost:4000

FIREBASE_CREDENTIALS_PATH=secrets/firebase-adminsdk.json
CELERY_BROKER_URL=redis://localhost:6380/1
CELERY_RESULT_BACKEND=redis://localhost:6380/1

# AI completion via isolated LM Studio proxy (OpenAI-compatible, no JWT)
# Prod Monica calls GPU host :8000; GPU host proxies to local LM Studio :1234
AI_COMPLETION_ENABLED=1
OPENAI_BASE_URL=http://193.105.37.142:8000/api/lmstudio/v1
OPENAI_API_KEY=
OPENAI_MODEL=qwen3-vl-8b-thinking
AI_MAX_TOKENS=200
AI_RATE_PER_MINUTE=20
AI_MIN_DRAFT_LEN=8
AI_REQUEST_TIMEOUT_SEC=45

# Only needed on the host that runs /api/lmstudio (GPU / LAN :8000)
LMSTUDIO_UPSTREAM_BASE_URL=http://127.0.0.1:1234/v1
LMSTUDIO_UPSTREAM_API_KEY=lm-studio
